L2 ask-policy — status after review remediation (harness code).

Previous shaped rows in l2_ask_policy.tsv / l2_ask_policy_d_current/ are
PROVISIONAL. They were collected before:

  1. Oscillation path actually finalized JSON (bail-before-finalize removed)
  2. Oscillation detector required consecutive evaluation adopts
  3. Ask→first-byte sampling (not ask→last-byte after full body)
  4. FIFO ask→response pairing (no overwrite / dropped samples)
  5. Control drain so bytes_on_wire counts all responses
  6. Windowed arms offering the same step cadence as control

Do not treat those rows as D26 evidence. Re-run the shaped campaign with the
fixed harness before drawing conclusions.

Original stop note (oscillation on dynamic rtt=60 loss=0.5 run=2) remains
historically true for that build; the detector itself did not match the brief.
