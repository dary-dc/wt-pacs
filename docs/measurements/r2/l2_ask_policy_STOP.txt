WITHDRAWN 2026-08-31: these rows do not measure ask policy. Read
l2_ask_policy_METHODOLOGY_REVIEW.md before quoting anything below.

L2 ask-policy — post-review shaped campaign COMPLETE.

Re-ran on Oracle SP with harness tip that includes review remediations
(ask→first-byte + FIFO pairing, consecutive-eval oscillation detector,
finalize-on-oscillation, control/windowed drain, matched step cadence,
TSV peak_outstanding/wait_samples/stream_mode).

Canonical outputs (this run):
  docs/measurements/r2/l2_ask_policy.tsv          — 54 raw rows, full grid
  docs/measurements/r2/l2_ask_policy_d_current/   — dynamic trajectories

Exit: CAMPAIGN_EXIT=0 (no oscillation stop, no void stop).
demand/supply offered at 16 ms/step ≈ 1.60 for all arms.

Prior provisional rows retained for audit only:
  l2_ask_policy_PROVISIONAL_pre_review.tsv
  l2_ask_policy_d_current_PROVISIONAL_pre_review/

Raw only — no interpretation in this file.

**Next:** `docs/lanes/L2-ask-policy-harness-fix.md` — ordered remediation + smoke gates.
