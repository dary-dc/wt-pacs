# Fixtures

What the server and the lab read, and how each is made. Generated bytes are gitignored; the
`metadata.json` beside them is tracked, so a set made at another size must have its metadata
committed with it. Every image is generated, never derived from a study: reproducible anywhere,
no provenance.

## SBND study bundle

What `exact-server --study` serves (`ingest/study-bundle`). All integers little-endian:

```text
magic "SBND" · version u32 (1) · metadata_len u32 · frame_count u32
frame_table[frame_count]: offset u64 · length u32
[UTF-8 metadata JSON]
[concatenated HTJ2K codestreams]
```

The server stores codestreams raw and adds the envelope at send ([`WIRE.md`](WIRE.md)). A bundle
with a table entry outside its data region fails to open.

Pack loose frames with `cargo run -p pack-study -- --metadata M.json --frames DIR --output
X.sbnd`: `metadata.json` must carry `frameCount`, and the frames are `DIR/000.htj2k`,
`001.htj2k`, …; `--sidecar PATH` also writes the metadata beside the bundle.

## The sets

| Where | What | How it is made |
| --- | --- | --- |
| `fixtures/us_cine_smoke/` | 3 frames, 19-byte placeholders (`htj2k-smoke-frame-N`), not HTJ2K; the bundle is tracked | `pack-study`, `README.md`; the default `--study` of `server/dev-server.py` and `deploy/` |
| `lab/fixtures/frames_32k/`, `frames_250k/` | 80 frames × 32 000 / 250 000 B of `0xAB` | `lab/scripts/gen_tf_fixtures.sh` (`FRAMES=` to change the count) |
| `lab/fixtures/frames_250k_live/` and the rung sets: `frames_16k_rung` (320 × 16 KiB), `frames_16k_seq` (65 536), `frames_16k_deep` (262 144), `frames_16k_big` (5 120), `frames_250k_big` (2 048 × 250 000), `frames_250k_deep` (32 000), `frames_32k_big` (2 000 × 32 000), `frames_tiny` (80 × 100 B) | constant-byte synthetic SBND, each with its own `README.md` | `NAME=… BYTES=… FRAMES=… lab/scripts/gen_live_cell_fixture.sh` (default: `frames_250k_live`, 320 × 250 000) |
| `lab/fixtures/queue_large/` | 20 frames of 41–61 KB (mean ~51 KB), **not valid HTJ2K**, realistic sizes; frames and bundle tracked | `pack-study`, its `README.md` |
| `lab/fixtures/decode_*/` | HTJ2K codestreams `NNN.j2c`, each with a `.sha256` of the samples a decoder must emit, for `lab/decode-bench` and the decode labs | `lab/scripts/gen_htj2k_fixtures.sh [size …]`, below |

Constant-byte and placeholder frames exercise the transport only: they do not decode, and they
compress like nothing real. The wire test in the gate makes its own study — 200 random 256 KB
frames — under a temp dir (`client/conformance/run_wire.sh`).

## HTJ2K sets

`OUT_ROOT=… FRAMES=… lab/scripts/gen_htj2k_fixtures.sh [size …]` (default 87 frames; `c512 g512
g1024 g2048` when no size is named). The decoder ships no encoder, so the script builds OpenJPH
from source once (`OJPH_TAG`, default 0.31.0) for `ojph_compress`. Every frame is encoded in the
profile the project serves: reversible 5/3, 5 levels, 64×64 blocks, RPCL, one layer, one tile.

| size | frame | content |
| --- | --- | --- |
| `g160`, `g256`, `g512`, `g1024`, `g2048` | 160² … 2048², 1 × 16-bit | `field`: 50 KB to 8 MB decoded, the ladder the copy-cost and heap work needs |
| `c512` | 512², 3 × 8-bit | `field` |
| `sat256` | 256², 1 × 16-bit | `ramp`: a full-range gradient hitting exactly 0 and 65 535, the only set that exercises a decoder's clamp |
| `s512`, `s12` | 512², 1 × 16-bit / 12-bit, **signed** | `field` |
| `cine512` | 512², 3 × 8-bit | `cine`: compresses like an ultrasound cine |
| `ct512` | 512², 1 × 12-bit signed | `ct`: compresses like CT |
| `warmup_c`, `warmup_g` | 160², colour / 16-bit grey, one frame | the client's warm-up frames (`client/downloader/warmup/`) |
| `warmup_c92`, `warmup_g277` | 92² colour, 277² grey, one frame | the other shape's sample count, for `lab/decoder-warmup` |

**Content decides the numbers.** `field` compresses about 1.25:1, which no modality does, and skews
every decode number toward block decoding; `cine` and `ct` land near the ratios their modalities
give ([`decode/README.md`](decode/README.md) §Content, and why it decides the numbers). Frames come
from `lab/scripts/gen_frame_pnm.py`, structured rather than random — random pixels do not compress
and a flat field compresses to nothing.

**Ground truth is the encoder's input**, never a decoder under test: `gen_frame_pnm.py` writes the
`.sha256` of the samples in decoder order, since an oracle built by the decoder shares its bugs.

**Signed sets are made the other way round**, because `ojph_compress -signed` makes frames its own
decoder does not survive: encode unsigned, then set each component's sign bit in SIZ
(`lab/scripts/sign_htj2k.py`). The same coded bits then decode to `v − 2^(B−1)`, which OpenJPEG's
`opj_decompress`, an independent decoder, confirmed on 16- and 12-bit before it was trusted; the
`.sha256` is of little-endian int16 samples, sign-extended ([`decode/README.md`](decode/README.md)
§Ground truth).
